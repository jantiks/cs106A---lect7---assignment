/*
this class displays golden ratio constant
 */

import acm.program.*;

public class goldenRatio extends ConsoleProgram {
    // runs program
    public void run() {
        double goldenRatio = (1 + Math.sqrt(5)) / 2;
        println(goldenRatio);
    }
}
